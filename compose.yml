services:
  db:
    image: postgres:14.3
    restart: unless-stopped
    ports:
      - "6432:5432"
    environment: 
      POSTGRES_PASSWORD: ${DB_PASSWORD} 
      POSTGRES_DB: ${DB_NAME}
    container_name: quertydb
    volumes:
      - ./postgres:/var/lib/postgresql/data
